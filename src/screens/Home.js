import React from 'react';
import { SafeAreaView, ScrollView, Text, View } from 'react-native';

const Home = () => {
  return (
  <SafeAreaView>
    <ScrollView>
    <View>
      <Text>Hello</Text>
    </View>
    </ScrollView>
  </SafeAreaView>
  ); 
};

export default Home;